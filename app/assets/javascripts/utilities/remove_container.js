window.addEventListener('load', () => {
    const main = document.getElementById('show-question-page')

    if (main) {
        const div = document.getElementById('main-container')
        div.classList.remove('container')
    }
})
