<h1><%= t('action.new_answer') %></h1>

<p>
  <%= t('.question') %>:
  <%= @answer.question.body %>
</p>


<%= render 'form', answer: @answer %>

<%= link_to t('action.back'), admin_question_path(@question) %>
