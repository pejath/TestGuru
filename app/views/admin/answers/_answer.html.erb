<li>
  <%= answer.body %><br>
  <%= link_to t('action.show'), admin_answer_path(answer) %> |
  <%= link_to t('action.edit'), edit_admin_answer_path(answer) %> |
  <%= link_to t('action.delete'), admin_answer_path(answer), method: :delete, data: { confirm: 'Are you sure?' } %>
</li>
